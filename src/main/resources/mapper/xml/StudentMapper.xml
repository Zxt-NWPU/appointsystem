<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.appointsystem.mapper.StudentMapper">

    <!-- 根据学号查询 -->
    <select id="selectByStudentId" parameterType="String" resultType="com.school.appointsystem.entity.Student">
        SELECT id, student_id AS studentId, name, gender, college, phone, birthday, remark
        FROM student
        WHERE student_id = #{studentId}
    </select>

    <!-- 插入学生 -->
    <insert id="insert" parameterType="com.school.appointsystem.entity.Student">
        INSERT INTO student (student_id, name, gender, college, phone, birthday, remark)
        VALUES (#{studentId}, #{name}, #{gender}, #{college}, #{phone}, #{birthday}, #{remark})
    </insert>

    <!-- 查询所有学生 -->
    <select id="selectAll" resultType="com.school.appointsystem.entity.Student">
        SELECT id, student_id AS studentId, name, gender, college, phone, birthday, remark
        FROM student
        ORDER BY student_id
    </select>

</mapper>
