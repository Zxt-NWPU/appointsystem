<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.appointsystem.mapper.RoomMapper">

    <!-- 根据编号查询 -->
    <select id="selectByRoomNo" parameterType="String" resultType="com.school.appointsystem.entity.Room">
        SELECT id, room_no AS roomNo, campus, building, room_number AS roomNumber
        FROM room
        WHERE room_no = #{roomNo}
    </select>

    <!-- 插入咨询室 -->
    <insert id="insert" parameterType="com.school.appointsystem.entity.Room">
        INSERT INTO room (room_no, campus, building, room_number)
        VALUES (#{roomNo}, #{campus}, #{building}, #{roomNumber})
    </insert>

    <!-- 查询所有咨询室 -->
    <select id="selectAll" resultType="com.school.appointsystem.entity.Room">
        SELECT id, room_no AS roomNo, campus, building, room_number AS roomNumber
        FROM room
    </select>

</mapper>
